# PartFolder 3D backend dependencies
# Python 3.12

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Settings / validation
pydantic-settings==2.7.0

# Job queue
arq==0.26.1

# Cryptography (secrets at rest, Phase 1)
cryptography==44.0.0

# Password hashing (Phase 1, adding deps now)
argon2-cffi==23.1.0
passlib[argon2]==1.7.4

# Storage / sidecar (Phase 2)
# python-slugify handles NFKD → ASCII transliteration + slug sanitization.
# text-unidecode is the transliteration back-end (non-GPL, MIT licensed).
python-slugify==8.0.4
text-unidecode==1.3
# PyYAML for sidecar read/write (already transitive via alembic; pinned here).
PyYAML==6.0.2

# Rendering (Phase 4)
# trimesh: parse STL/OBJ/PLY/3MF; vtk: offscreen CPU render (Mesa software rasterizer).
# pyrender+PyOpenGL: EGL/OSMesa path (tried first; falls back to vtk when unavailable).
# Pillow: image I/O helper. numpy: mesh math.
trimesh==4.5.3
pyrender==0.1.45
Pillow==11.1.0
# pyrender declares ==3.1.0 but functionally works with newer versions.
# >=3.1.7 is required for OSMesaCreateContextAttribs (pyrender OSMesa platform).
PyOpenGL>=3.1.0
vtk==9.3.1
numpy>=1.24.0,<3.0

# Import wizard / URL scraping (Phase 5)
# httpx is already listed under Dev/testing (used in tests); keep it there.
# selectolax: fast CSS-based HTML parser for scraping Open Graph / meta tags.
selectolax==0.4.10
# python-multipart: required by FastAPI for the Add Asset multipart file upload.
python-multipart==0.0.20

# Dev / testing
ruff==0.8.4
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
anyio[trio]==4.7.0
pydantic[email]
