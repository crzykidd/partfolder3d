---
name: 2026-06-30-docs-pass
status: done
created: 2026-06-30
model: sonnet
completed: 2026-06-30
result: All 5 doc targets updated (README features + getting-started, .env.example, build-plan.md, new features-overview.md, new nav-architecture.md) plus decisions.md one-liner. No code files touched.
---

# Task: Documentation pass — bring user-facing docs current (post nav-reorg, final tab names)

Update the docs to reflect the features shipped this cycle and the **new admin nav** (5 tabbed
sections, final routes below). Docs/markdown ONLY — do NOT change app code or tests.

## Final admin nav / routes (use these names + paths in all docs)
5 sections (one admin nav group): **Content** (`/admin/content` → Libraries, Tags, Print Stats) ·
**Users & Access** (`/admin/access` → Users, Invites, Password Resets) · **AI & Scraping**
(`/admin/ai` → AI Providers, AI Usage, Site Capabilities) · **Jobs & Activity** (`/admin/activity` →
Jobs, Scheduled, Reviews, Issues, Change Log) · **Data & Backups** (`/admin/data` → Backups, Export,
Share Audit). Old `/admin/*` paths still redirect, but docs should reference the **new** ones (and
prefer feature names over deep menu paths where possible). Non-admin nav unchanged: Library (Catalog/
Favorites/My Creations), Import (Add Asset/Imports), and a personal group (Quick Start, Settings, API
Keys).

## 1. `README.md`
- **Features section** — add the 8 features shipped this cycle (group under the right headings):
  - AI: **optional AgentQL fallback scraper** for Cloudflare-gated sites (e.g. MakerWorld), BYO key +
    local budget control (free-allowance / monthly $ cap); **AI usage tracking with per-provider $
    cost estimates**.
  - Rendering/Images: **renders surfaced as gallery images**; **per-item image upload/delete**; item
    **delete-to-trash** (recoverable).
  - New "**Asset analysis**": per-object **estimated filament grams + color count** for STL/3MF
    (volume-based estimate; low-confidence flag for non-watertight meshes).
  - New "**Modification tracking**": local-modified-vs-original detection + a public-share "modified
    copy" notice.
  - Tagging: **tag delete** (untags items, never deletes them), **typeahead autocomplete** in the
    import wizard, **starter-tags loader**, tag-cloud **Alpha/Number sort** + in-use-only counts.
  - Storage/paths: **per-library × per-OS local path prefixes** (auto-picks Windows `\` / Linux·mac `/`).
  - Ops: **failed-job retry**; **import management** (delete session / delete session image / inbox dir).
  - UI: Aurora revamp — **switchable top/side nav** (per-user), **customizable widget dashboard**,
    **Quick Start** onboarding page, **5-section admin nav**.
- **Getting started** — make it match the real flow and avoid stale menu paths:
  `docker compose -f docker-compose.dev.yml up -d --build` → first-run wizard (admin + instance
  basics) → add a library on the **Libraries** page (Content section) → *(optional, skippable)* Load
  Starter Tags, enable an AI provider, schedule backups. Remove any "Admin → Libraries"-style menu
  refs and the outdated "seed tags / Settings later" wording.

## 2. `.env.example`
- Replace the 3× "Admin → Libraries" references with neutral wording ("the **Libraries** admin page",
  under **Content**). Keep the rest (library mount guidance, AgentQL note if present) accurate.

## 3. `docs/build-plan.md`
- Currently says "Status: not started, next = Phase 0" — factually wrong. Update to: phases 0–10
  shipped in the v0.1.0 alpha; the post-revamp feature/fix run + modularization refactor + nav reorg
  are on `dev`; full history in `docs/decisions.md`. Keep it short.

## 4. New `docs/features-overview.md`
- A concise reference to the new features (one short para each, with the admin section/route where
  it's configured): AgentQL fallback, AI usage+cost, asset analysis, modification tracking, per-
  library/OS path prefixes, image management + renders-as-images, tag improvements, import
  management, Aurora UI + nav. Useful for release notes + onboarding.

## 5. New `docs/nav-architecture.md`
- Document the final IA: the 5 admin sections, their tabs, and the route map (table). Note the
  back-compat redirects and that new admin features should slot into the right section/tab. Helps
  future devs.

## Out of scope
- `CHANGELOG.md` finalization happens in `/release-prep` (don't rewrite it here). PRD, atomic-moves,
  sidecar-schema, decisions, images are current — leave them.

## Verify
- Docs only — no code. Sanity-check markdown renders (no broken tables), and that any routes/paths you
  cite match the route map above. No tsc/tests needed (touch no code); if you accidentally touch any
  `.ts/.tsx`, revert it.

## When done
1. Frontmatter; `git mv` to `prompts/done/`.
2. `docs/decisions.md`: one line noting the docs refresh (README features + getting-started, .env,
   build-plan, + new features-overview/nav-architecture).
3. **Do NOT commit/push/branch, NEVER `git add -A`.** Report back: file list; one-line `docs:` commit
   message; confirmation no code files were touched; anything unverified.
