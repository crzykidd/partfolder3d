---
name: 2026-07-02-15-bulk-import
status: completed
created: 2026-07-02
model: sonnet
completed: 2026-07-02
result: Shipped bulk-commit endpoint, default-import-library setting, and Commit-ready UI button on ImportsPage; 33 backend tests + 8 frontend tests passing, ruff + build clean
---

# Task: Bulk import (issue #15) — bulk-commit endpoint + default-library setting + UI button

Ship the bulk-commit endpoint, default import library setting, and Commit-ready button on the Imports page. No CLI (deferred per owner decision).

## Before you start

- Frontend stack: Tailwind + CSS-var theme + minimal Radix + lucide + TanStack Query + apiFetch/apiFetchForm CSRF. NO Mantine, NO toast lib.
- Verify: `npm run build` (tsc -b && vite build) + `npx vitest run`. Backend: `backend/.venv/bin/ruff check backend/` (pinned 0.8.4). Backend tests need Postgres on localhost:5433.

## Working tree check

Run `git status --porcelain` first; report dirty files before touching.

## What to do

1. Implement backend refactor, new endpoint, setting validation, inbox-scan library auto-set
2. Implement frontend types, API function, Imports page button + modal
3. Write scoped backend tests and frontend vitest
4. Update CHANGELOG.md [Unreleased] and docs/decisions.md
5. Verify (ruff, scoped pytest, npm run build, vitest)

## When done

Update frontmatter, `git mv` to prompts/done/, include in single feat: commit.
