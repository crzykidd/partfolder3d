# Runtime-generated by nginx/40-partfolder-tls.sh — do not edit by hand,
# your changes are overwritten on every container start.
#
# Baked default: empty (plain HTTP passthrough). This file is `include`d near
# the top of the :80 server in nginx.conf. When TLS_MODE != off AND
# TLS_REDIRECT=true, the TLS entrypoint script rewrites it to a 301 redirect
# to HTTPS; otherwise it stays empty.
