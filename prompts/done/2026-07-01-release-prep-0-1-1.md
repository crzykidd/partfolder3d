---
name: 2026-07-01-release-prep-0-1-1
status: completed
created: 2026-07-01
model: sonnet            # release prep — changelog + version + docs
completed: 2026-07-01
result: >
  Version bumped to 0.1.1 in backend/app/version.py + frontend/package.json.
  CHANGELOG: [0.1.1] section authored (Added: render mode/subprocess/recovery,
  job lifecycle, issue resolution, catalog polish; Fixed: 4 bugs); [0.1.0] dated.
  README: badge, What's New v0.1.1 entry, and stale version refs updated.
  CLAUDE.md: status line updated. Validations: ruff clean, tsc clean, vitest
  229/229, vite build clean, both compose configs valid. Committed as
  chore(release): prepare v0.1.1. No push, no tag.
---

# Task: Release prep for v0.1.1 (author changelog, bump version, sync docs)

Prepare the v0.1.1 release. Current version is `0.1.0` (pre-seeded, never tagged). The
`CHANGELOG.md` already has a comprehensive `## [0.1.0] — unreleased` section (Phases 0–10) and an
EMPTY `## [Unreleased]`. This release bumps to **0.1.1**, whose notes are **this session's work**
(listed below). The `[0.1.0]` section stays as the initial-build baseline. Bare semver everywhere;
the `v` prefix is added only at tag time (not here). **Do NOT push, do NOT open a PR, do NOT tag** —
prepare the working tree + ONE commit and report the proposed PR title/body back; the orchestrator
does the push + PR.

## Before you start
- Read `prompts/startnewsession.md` and `CLAUDE.md`. Spawned agent on `dev`. Do NOT edit
  `docs/decisions.md`. Today's date is **2026-07-01**.
- Working tree must be clean before you start (`git status --porcelain` empty) — if not, STOP.

## This session's commits → author as the [0.1.1] changelog entry
Turn these into user-facing Keep-a-Changelog prose (categorised Added / Changed / Fixed /
Security). Group sensibly; drop purely-internal noise (CI tooling, a test fix, the docs refresh)
or fold it into a terse "Changed/internal" note — judgment call, keep it user-facing.

```
feat: RENDER_MODE admin setting (render.mode: all / no_images / off)           [b023b53]
feat: renders run in killable subprocess + thread caps + timeout + crash recovery [45a40b3]
feat: job lifecycle — cancel/restart/supersede/archive/retention (migration 0019) [28f79a4]
feat: job monitor cancel/restart/archive/clear controls + archive view          [e0e1375]
feat: job-monitor clear button contextual to the active status filter           [1311eb6]
feat: issue resolution framework — dedup + orphan actions (migration 0020)       [0299841]
feat: issue-resolution action buttons UI (ignore/delete/import + sidecar prefill) [f041cd9]
feat: Phase 3 issue resolution — per-type corrective actions (backend)           [e3c2091]
feat: issue-resolution action labels/icons for all corrective actions (UI)       [ef0a387]
feat: make stat strip tiles clickable links to their detail pages                [4c7d1a0]
feat: sortable Category/Uses columns on admin Tags table                         [f2e704e]
fix: scraped-image filename collision on import commit (MakerWorld)              [8c1c895]
fix: gentler tag-cloud font scaling (max ~14px)                                  [72ede1b,3aabf31]
fix: sidebar highlights the active admin section across sub-tabs                 [3a17711]
fix: native dropdowns/scrollbars render dark in dark theme                       [9f149dc]
ci: pip/npm caching + split frontend into its own parallel job                   [d756183]  (internal)
fix: AgentQL usage-window test deterministic on the 1st of month                 [9030f89]  (internal/test)
docs: README + features-overview refresh                                          [dc0f8d0]  (internal/docs)
```

## What to do

1. **Version bump.** `backend/app/version.py`: `__version__ = "0.1.1"`.
   `frontend/package.json`: `"version": "0.1.1"`. (Only these two.)
2. **CHANGELOG.md:**
   - Author a new `## [0.1.1] — 2026-07-01` section (this session's work, categorised) placed
     **between** `## [Unreleased]` and `## [0.1.0]`. Keep `## [Unreleased]` present but empty
     (matching the file's existing skeleton/placeholder style).
   - Change the existing `## [0.1.0] — unreleased` header to `## [0.1.0] — 2026-07-01` (it was
     never tagged; it ships as the baseline of the first tagged release). Do NOT alter its body.
   - Leave the "Archived releases" footer note as-is.
3. **README.md:**
   - Version badge: replace `version-0.1.0-0FA4AB` with `version-0.1.1-0FA4AB`.
   - Add a `### v0.1.1 (2026-07-01)` entry at the top of `## What's New`, summarising this
     release in user-facing language (render controls, job lifecycle/monitor, issue resolution,
     catalog polish, key fixes). Match the existing voice.
4. **CLAUDE.md:** the top `> **Status:**` line currently says alpha / v0.1.0 unreleased — update
   it to reference `0.1.1` (keep the alpha framing if present). Only adjust the version reference.
5. **Validate (CPU-CAPPED — a prior run buried the host CPU):**
   - `backend/.venv/bin/ruff check backend/` (from repo root).
   - `cd frontend && npx tsc --noEmit && npx vitest run && npx vite build`.
   - `docker compose config --quiet && docker compose -f docker-compose.dev.yml config --quiet`.
   - Backend pytest + migration require a live PG — do NOT run the full suite here; note they are
     covered by CI. (A capped `alembic upgrade head` on an ephemeral PG is fine if you want, torn
     down after — optional.)
   - Grep for version drift: no stray `0.1.0` left in `README.md` badge, `version.py`,
     `package.json`, `CLAUDE.md` (the CHANGELOG `[0.1.0]` section is expected to remain).
6. **One commit** (do NOT push): stage everything and commit:
   ```
   chore(release): prepare v0.1.1

   - backend/app/version.py + frontend/package.json bumped to 0.1.1
   - CHANGELOG: added [0.1.1] — 2026-07-01 (this session's work); dated [0.1.0]
   - README: version badge + What's New entry
   - CLAUDE.md: Status line updated to 0.1.1
   ```
   No `Co-authored-by` lines.

## When done
1. Update this prompt's frontmatter (`status: completed`, `completed: 2026-07-01`, `result`).
2. `git mv` it into `prompts/done/`.
3. Do NOT edit `docs/decisions.md`.
4. Report back: files changed, the validation results, the exact **PR title** (`Release v0.1.1`)
   and **PR body** (the full `[0.1.1]` changelog section verbatim, so the orchestrator can open
   the PR), and confirm you did NOT push/tag.
